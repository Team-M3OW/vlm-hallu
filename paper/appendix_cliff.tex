% Appendix fragment: the size cliff, in three experiments.
% Included from main.tex via \input{appendix_cliff}; uses the macros defined there.

\section{The size cliff}\label{app:cliff}

\paragraph{Protocol.} Every measurement in this appendix uses \vstar\ with its ground-truth boxes, on
Qwen3-VL-2B and Qwen2-VL-7B unless stated. A \emph{merged visual token} is one post-merge patch
embedding; at a $300$-token pass it covers $1/300$ of the image area. A target's extent is $S\times
B$ merged tokens, where $S$ is the ground-truth box area as a fraction of the image and $B$ the
realised token count; its side is $\sqrt{S\times N}$ tokens. The \emph{oracle crop} replaces the
model's window with the ground-truth box at the same $300$-token budget and is a control, not a
method. Thresholds come from a sliding sweep: for each $t$, accuracy below and at-or-above $t$ is
compared on single-region items, requiring at least ten items per side, and $t^*$ maximises the
step. All intervals are paired item bootstraps ($8000$ resamples).

\paragraph{The cliff.} Accuracy as a function of the target's extent, with the oracle crop as the
difficulty control, is the central measurement of this appendix. The uniform curve is flat and near
chance for the smallest targets, dips \emph{below} the $25\%$ chance level, and rises steeply
through the shaded $0.15$--$0.25$ merged-token band; the oracle curve is flat at
$95.8\%$/$100.0\%$ across the same range. The two models' curves differ in level but not in shape or
in where the step sits. The step is sharpest at $t=0.15$ on the cleanly-localised single-region
subset, where the intervals are disjoint, and it appears at the same $0.25$-token threshold on all
single-region items of both models.

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{figs/fig_cliff_curve.pdf}
\caption{\textbf{The encoding cliff.} Accuracy vs target extent in merged tokens ($B_0{=}300$), with
item-bootstrap intervals. Red: uniform encoding; green: oracle crop (same question, same budget,
target resolved). Shaded band: the $0.15$--$0.25$ merged-token cliff. The uniform curve passes
\emph{below} the chance line before the threshold and through it at the threshold; the oracle curve
is flat.}
\label{fig:cliffcurve}
\end{figure}

\begin{table}[t]
\caption{\textbf{The encoding cliff.} Single-region localised items ($n{=}63$, Qwen3-VL-2B); the
oracle crop is flat across the same split ($100.0\%$ below, $97.6\%$ above).}
\label{tab:cliff}
\begin{center}
\small
\begin{tabular}{cccccc}
\multicolumn{1}{c}{\bf $t$ (merged tokens)}  &\multicolumn{1}{c}{\bf $n$ below} &\multicolumn{1}{c}{\bf acc below} &\multicolumn{1}{c}{\bf $n$ above} &\multicolumn{1}{c}{\bf acc above} &\multicolumn{1}{c}{\bf step}
\\ \hline \\
0.10 & 16 & 18.8\% & 47 & 72.3\% & +53.6\\
\textbf{0.15} & 21 & \textbf{19.0\%} & 42 & \textbf{78.6\%} & \textbf{+59.5}\\
0.25 & 33 & 33.3\% & 30 & 86.7\% & +53.3\\
0.50 & 46 & 47.8\% & 17 & 88.2\% & +40.4\\
1.00 & 57 & 56.1\% & 6 & 83.3\% & +27.2\\
\end{tabular}
\end{center}
\end{table}

\begin{table}[t]
\caption{\textbf{The cliff is architecture-invariant.} All single-region items, $n{=}115$ each;
intervals are disjoint on both models, and the oracle crop is flat and identical.}
\label{tab:cliff2}
\begin{center}
\small
\begin{tabular}{lccccc}
\multicolumn{1}{c}{\bf model}  &\multicolumn{1}{c}{\bf cliff} &\multicolumn{1}{c}{\bf uniform below} &\multicolumn{1}{c}{\bf uniform above} &\multicolumn{1}{c}{\bf oracle below} &\multicolumn{1}{c}{\bf oracle above}
\\ \hline \\
Qwen3-VL-2B & 0.25 tok & 32.4\% & 77.3\% & 95.8\% & 100.0\%\\
Qwen2-VL-7B & 0.25 tok & 36.6\% & 63.6\% & 95.8\% & 100.0\%\\
\end{tabular}
\end{center}
\end{table}

\paragraph{It is not the budget.} The natural objection is that the cliff is simply a budget effect:
spend more tokens and the target stops being sub-token. It is not. Recomputing the sliding threshold
at $300$ and $588$ tokens shows that the threshold in merged tokens scales with the budget
($0.25\to0.55$ on Qwen3) while its image-side fraction is unchanged ($2.89\%\to3.06\%$); Qwen2 is
noisier but agrees in direction. In other words, the criterion is the target's \emph{relative size},
not the number of tokens that land on it: at a fixed tokens-on-target, accuracy differs between the
two budgets (bin $[0.15,0.25)$: $33.3\%$ vs $53.8\%$ on Qwen3; $41.7\%$ vs $61.5\%$ on Qwen2), so the
two budgets do not collapse onto one curve. Uniform budget cannot substitute for magnification
because it leaves the target's fraction of the image unchanged.

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{figs/fig_cliff_budget.pdf}
\caption{\textbf{The cliff does not move with budget.} Accuracy vs the target's side as a fraction of
the image, at $300$ and $588$ tokens, both models. The step sits at the same relative size
($\approx3\%$ of the side) at both budgets: doubling the uniform budget moves the token threshold but
not the cliff.}
\label{fig:cliffbudget}
\end{figure}

\begin{table}[t]
\caption{\textbf{The cliff does not move with budget.} Single-region items; $t^*$ maximises the
below/above step.}
\label{tab:cliffbudget}
\begin{center}
\small
\begin{tabular}{llccccc}
\multicolumn{1}{c}{\bf model}  &\multicolumn{1}{c}{\bf budget} &\multicolumn{1}{c}{\bf $t^*$ (tok)} &\multicolumn{1}{c}{\bf acc below} &\multicolumn{1}{c}{\bf acc above} &\multicolumn{1}{c}{\bf $t^*/B$ (area)} &\multicolumn{1}{c}{\bf side fraction}
\\ \hline \\
Qwen3-VL-2B & 300 & 0.250 & 32.4\% & 75.0\% & 0.083\% & 2.89\%\\
Qwen3-VL-2B & 588 & 0.550 & 50.0\% & 87.2\% & 0.094\% & 3.06\%\\
Qwen2-VL-7B & 300 & 0.850 & 40.4\% & 75.0\% & 0.283\% & 5.32\%\\
Qwen2-VL-7B & 588 & 0.750 & 50.0\% & 77.4\% & 0.128\% & 3.57\%\\
\end{tabular}
\end{center}
\end{table}

\begin{table}[t]
\caption{\textbf{No collapse: accuracy at fixed tokens-on-target, by budget.} Qwen3-VL-2B /
Qwen2-VL-7B.}
\label{tab:collapse}
\begin{center}
\small
\begin{tabular}{lcccc}
\multicolumn{1}{c}{\bf tokens-on-target}  &\multicolumn{1}{c}{\bf acc @300 (Q3/Q2)} &\multicolumn{1}{c}{\bf acc @588 (Q3/Q2)}
\\ \hline \\
$[0,0.15)$ & 31.9 / 31.9 & 48.5 / 39.4\\
$[0.15,0.25)$ & 33.3 / 41.7 & 53.8 / 61.5\\
$[0.25,0.50)$ & 73.7 / 63.2 & 52.0 / 60.0\\
$[0.50,1.00)$ & 80.0 / 60.0 & 80.0 / 65.0\\
$[1.00,2.00)$ & 66.7 / 50.0 & 78.6 / 78.6\\
\end{tabular}
\end{center}
\end{table}

\paragraph{Magnification is not the bottleneck.} One control bounds the interpretation: the tightest
oracle window tested, $W{=}0.15$, resolves $100\%$ of items on both models in every size quartile
(median $W^*=0.15$; rank correlation with $\sqrt{S}$ $\approx0$). Once the region is isolated, even a
$6.7\times$ crop reads the smallest detail, so what limits the model is isolating the region and the
target's relative size, not the amount of magnification.

\section{The information is never encoded}\label{app:encoding}

\paragraph{Protocol.} An item is a \emph{certified encoding failure} when it is wrong under the
uniform bar and right under the oracle crop at the same budget; this is the label no other work can
construct, because it needs the crop control. The layer-wise read-out is a logit lens: intermediate
layers are decoded as $\mathrm{lm\_head}(\mathrm{final\_norm}(h_\ell))$, while the final layer is
read from the model's own logits, because applying the final norm to the last hidden state as well
double-normalises that layer alone and corrupted an earlier result of ours (\S\ref{app:mech}); we
assert per run that the last-layer read-out matches the true logits. Representation patching replaces
the image tokens' hidden states at a single layer's output with those of a donor image carrying the
same question at the same token budget; the pre-registered controls are that patching the image at
L4 must be large and patching text at L20 must be large.

\paragraph{The answer is not in the stack.} If the answer were present but mis-routed, a layer-wise
read-out would find it somewhere and lose it later. Under the uniform encoding no layer's decoded
answer exceeds $52.9\%$, and localisation quality does not change that: on items where the window
covers the target and the model still answers wrong ($n{=}36$), the correct option is the argmax at
some layer on $72.2\%$ of items, against $68.1\%$ where the window missed entirely. There is no
layer at which the answer is present and subsequently lost: the apparent signal at L20 ($41.7\%$)
collapses to $0\%$ from L22 and matches what near-chance layers produce by chance. Under the oracle
crop the lens instead reaches $92.6\%$, and the answer appears abruptly at L22, taking $90\%$ of the
gain in a single transition; the step replicates one layer later on Qwen2-VL-7B and appears only
where the crop covers the target ($+23.8$ points covered, exactly $0.0$ where it misses). Cropping
does not make the model reason better; it supplies information that a fixed depth converts into an
answer. Representation patching agrees from the other side: replacing every image token's hidden
state with a different image's, at a single layer, changes the answer no more than replacing
irrelevant early text once past the transport boundary (KL $0.009$ vs $0.009$ on Qwen3 and $0.005$
vs $0.006$ on Qwen2 at L16). The oracle crop fixes $94.4\%$ of the localised-but-wrong items at the
same budget, with a median of $0.14$ merged tokens on target, and across the benchmark
$37.7\%$/$41.4\%$ of all items are certified encoding failures: $86.7\%$/$84.0\%$ of every error,
rising to $94.0\%$/$89.4\%$ at $W{=}0.15$.

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{figs/fig_lens.pdf}
\caption{\textbf{The answer is not in the stack.} Per-layer logit-lens accuracy under the uniform
encoding (red) and the oracle crop (green), both models. Uniform: flat, no layer above $52.9\%$ and
none holding an answer that is later lost. Oracle: the answer becomes decodable abruptly at L22,
taking most of the gain in one transition; the Qwen2 step is one layer later.}
\label{fig:lens}
\end{figure}

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{figs/fig_lens_example.pdf}
\caption{\textbf{The lens on a real item.} \vstar\ \texttt{direct\_attributes/96}: ``What is the color
of the bottle cap?'' (A) blue (B) white (C) red (D) orange. The target is a $12\times11$ px region,
$0.01$ merged tokens at the $300$-token pass. Top: the image as the uniform pass sees it (target
circled) and the oracle crop. Middle: the decoded probability of the correct option per layer.
Bottom: the decoded top-1 option letter at every layer, both arms. Under the uniform encoding the
correct option is decoded at \emph{no} layer; after the crop it appears at L22 and stays.}
\label{fig:lensexample}
\end{figure}

\paragraph{Why the target is invisible.} The arithmetic behind the threshold is simple. At the
$300$-token pass the median single-region target's side is $0.210$ merged tokens; $100\%$ of targets
are smaller than one merged token, the median target occupies $0.176$ tokens of area, and $62\%$ sit
below the $0.25$-token cliff. The evidence is averaged into a single token's footprint together with
its background before the language model ever sees it --- which is why magnification, not budget, is
the operation that recovers it.

\begin{figure}[h]\centering
\includegraphics[width=0.62\textwidth]{figs/fig_footprint.pdf}
\caption{\textbf{The target is smaller than a token.} Distribution of the single-region target's side
in merged-token units at the $300$-token pass, both models. Every target lies left of the one-token
line; most lie below the $0.25$-token cliff.}
\label{fig:footprint}
\end{figure}

\paragraph{Controls.} Two further controls bound the interpretation. \emph{Localisation degrades
below the cliff, far less than accuracy}: ground-truth coverage falls from $0.637$ to $0.457$
(argmax window) and from $0.751$ to $0.580$ (head window) on Qwen3, and $0.594\to0.450$ /
$0.620\to0.471$ on Qwen2, while accuracy falls by $42.6$ points --- encoding dominates, placement
contributes a minority share. \emph{The below-chance failure is not the language prior}: a text-only
control (question without the image) agrees with the image-conditioned answer on $49.4\%$ of
below-cliff items vs $40.7\%$ above on Qwen3, but $49.4\%$ vs $56.5\%$ on Qwen2 --- opposite
directions, neither interval clear. Below the cliff the model is systematically misled, but not by
the text-only prior.

\section{Allocation beats budget}\label{app:allocation}

\paragraph{Protocol.} The \emph{exchange rate} compares an oracle crop at the base budget with the
best uniform rung each model can reach; a random crop of the same size bounds the placement effect
from below. No uniform rung reaches the crop arm on any model, so every rate is a lower bound set by
the model's token ceiling; LLaVA-NeXT has no resolution axis and is reported as such. The
native-resolution comparison runs the same items with the uniform arm free to spend the model's full
dynamic range, and the boundary test runs HR-Bench 4K, where targets are large enough to be
resolvable at the base budget.

\paragraph{Allocation vs budget.} The exchange rate is the ratio of the token counts at which the two
arms score what they score. An oracle crop at $300$ tokens reaches $97.4\%$ on Qwen3-VL-2B and
$93.2\%$ on Qwen2-VL-7B, while no uniform allocation on any rung up to $7{,}776$ tokens reaches the
crop arm (Qwen2's ceiling is $75.4\%$); the implied exchange rate is $\ge$26$\times$ and
$\ge$25.9$\times$ the same tokens spent uniformly, with the placement effect itself worth $+40.0$
and $+59.2$ points. At native resolution the same holds: a perfectly placed $600$-token crop beats
native dynamic resolution ($3{,}290$ tokens, $5.5\times$ the budget) by $+11.6$/$+16.2$ pooled. The
boundary is measured rather than assumed: allocation pays when the evidence is smaller than the
budget can resolve --- $71\%$ of \vstar\ targets are sub-token and oracle allocation is worth $+37.2$
points --- and costs when it is not, as on HR-Bench 4K, where allocation loses $8.8$ points because
discarded context dominates. The localizer is good in both regimes; what changes is whether acting
on it helps.

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{figs/fig_exchange.pdf}
\caption{\textbf{Allocation vs budget.} Oracle crop at the base budget (green) against the best
uniform rung each model reaches (red; token counts annotated). The exchange rate is the ratio of the
token counts at which the two arms score what they score; no uniform rung matches the crop, so each
rate is a lower bound.}
\label{fig:exchange}
\end{figure}

\begin{table}[t]
\caption{\textbf{Allocation vs budget.} Placement effect = oracle crop $-$ random crop.}
\label{tab:exchange}
\begin{center}
\footnotesize\setlength{\tabcolsep}{2pt}
\begin{tabular}{lccccc}
\multicolumn{1}{c}{\bf model}  &\multicolumn{1}{c}{\bf oracle crop} &\multicolumn{1}{c}{\bf random crop} &\multicolumn{1}{c}{\bf placement effect} &\multicolumn{1}{c}{\bf uniform ceiling} &\multicolumn{1}{c}{\bf exchange rate}
\\ \hline \\
Qwen2-VL-7B & 93.2\% @300 & 34.0\% & +59.2 & 75.4\% @7{,}776 & $\ge$25.9$\times$\\
LLaVA-OneVision-7B & 86.9\% @1{,}323 & 37.7\% & +49.2 & 69.6\% @5{,}157 & $\ge$3.9$\times$\\
LLaVA-NeXT-7B & 80.6\% @1{,}176 & 38.2\% & +42.4 & 38.2\% @1{,}464 & no axis\\
Qwen3-VL-2B & 97.4\% @300 & --- & +40.0 (attn$-$rand) & --- & $\ge$26$\times$\\
\end{tabular}
\end{center}
\end{table}

\paragraph{What did not extend the crop.} Two attempts to buy the crop out of its cross-instance
limit failed, and both are charged in full. Adding the original image back costs $1.5\times$ the bar
once the localisation pass is counted and scores below the bar-matched crop alone on every stratum
(pooled $+4.2$ vs $+10.5$; cross $-10.5$ vs $-1.3$); the original dilutes the crop's attention more
than it restores the second object. Two full-resolution crops, funded by early-exit localisation and
boundary pruning at $101$--$104\%$ of the bar, help single further ($80.9$ vs $62.6$) but do not
clear on cross ($68.4$ vs $65.8$, $0$ of $2$ pre-registered).

\begin{table}[t]
\caption{\textbf{Extensions of the crop, all charged in full.} Qwen3-VL-2B, \vstar, $n{=}191$; deltas
against the equal-compute bar. The funded 2-crop's pre-registered criterion did not pass ($0$ of
$2$).}
\label{tab:cropx}
\begin{center}
\small
\begin{tabular}{lcccc}
\multicolumn{1}{c}{\bf arm}  &\multicolumn{1}{c}{\bf cost vs bar} &\multicolumn{1}{c}{\bf pooled} &\multicolumn{1}{c}{\bf single} &\multicolumn{1}{c}{\bf cross}
\\ \hline \\
DWA crop (reference) & $1.0\times$ & \gd{$+10.5$} & \gd{$+18.3$} & $-1.3$\\
crop $+$ original & $1.5\times$ & $+4.2$ & $+13.9$ & \bad{$-10.5$}\\
2 crops $+$ original & $1.5\times$ & $+7.3$ & $+13.9$ & $-2.6$\\
4 crops $+$ original & $1.5\times$ & $+5.8$ & $+11.3$ & $-2.6$\\
funded 2-crop & $1.01$--$1.04\times$ & --- & \gd{$+18.3$} & $+2.6$\\
\end{tabular}
\end{center}
\end{table}
