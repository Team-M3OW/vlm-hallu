# Teammate's paper — under double-blind review. Do not publish.
/main.pdf
*.pdf
# ...but our own paper and its figures are ours to publish
!paper/iclr2027_submission.pdf
!paper/figs/*.pdf

# LaTeX build artifacts
*.aux
*.fdb_latexmk
*.fls
*.log
*.out

# Experiment artifacts: large, regenerable from scripts/, and some exceed GitHub's 100MB limit
data/
*.npz
*.jsonl
*.joblib

# Local scratch
__pycache__/
*.pyc
.ipynb_checkpoints/

# Model checkpoints and archives — exceed GitHub's 100MB file limit; pull from HF instead
models/
*.safetensors
*.bin
*.tgz
data/phase173_*heads.npz
